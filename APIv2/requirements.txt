# FastAPI framework
fastapi>=0.115.0

# ASGI server for development
uvicorn[standard]>=0.32.0

# Configuration management
pydantic-settings>=2.6.0
pydantic[email]>=2.0.0

# Database
sqlalchemy>=2.0.0
asyncpg>=0.30.0
alembic>=1.14.0

# Authentication & Security
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0
types-python-jose
python-multipart>=0.0.6

# Vectorization
pgvector

bcrypt==4.0.1

passlib[bcrypt]==1.7.4
types-passlib

# Testing
pytest
pytest-asyncio
httpx
aiosqlite

# Azure Services
azure-storage-blob>=12.19.0      # BlobServiceClient, generate_blob_sas
azure-identity>=1.15.0

# OpenAI
openai>=1.12.0                   # Client OpenAI avec support vision et embeddings

# Image Processing
Pillow>=10.0.0                   # Manipulation d'images, validation format/taille

# Queue/Background Processing
redis>=5.0.0                     # Client Redis pour la queue
celery>=5.3.0                    # Task queue (optionnel, alternative à custom queue)
# OU
rq>=1.15.0                       # Simple Redis Queue (plus léger que Celery)

# File Upload
python-multipart>=0.0.6          # Déjà présent, pour FastAPI file uploads

# Type hints
types-redis                      # Type hints pour Redis

aiohttp
starlette